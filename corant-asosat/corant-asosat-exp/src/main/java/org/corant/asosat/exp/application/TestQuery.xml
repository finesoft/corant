<?xml version="1.0" encoding="UTF-8"?>
<query-mappings
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<common-segment>
		This is common segment!
	</common-segment>
	
	<query name="Test.get"
		result-class="java.util.Map" cache="true"
		cache-resuleset-metadata="false">
		<script>
			<![CDATA[
				SELECT
					id
					,vn
					,mn
					,name
				FROM 
					EP_Test
				WHERE
				    1<>1
				    OR
				    id = ${SP(id)}
			]]>
		</script>
	</query>

	<query name="Test.query" result-class="java.util.Map"
		cache="true" cache-resuleset-metadata="false">
		<script>
			<![CDATA[
				SELECT
					id
					,vn
					,mn
					,name
				FROM 
					EP_Test
				WHERE
				   1=1
				<#if keyword??>
					AND (name LIKE '%' + ${SP(keyword)} + '%')
				</#if>
				ORDER BY
					id desc
			]]>
		</script>
	</query>
</query-mappings>